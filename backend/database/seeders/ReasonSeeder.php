<?php

namespace Database\Seeders;

use App\Models\Reason;
use Illuminate\Database\Seeder;

class ReasonSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $reasons = [
        //     "Current country changed",
        //     "Use wrong password 5 times within a hour",
        //     "Reset password 5 times within a hour",
        //     "Delete a user",
        // ];

        // foreach ($reasons as $reason) {
        //     Reason::factory()->create(["title" => $reason]);
        // }
    }
}
